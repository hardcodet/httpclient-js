{
  "name": "@hardcodet/httpclient",
  "version": "1.0.2",
  "description": "HTTP client library for easy consumption of RESTful APIs. Comes with built-in authentication strategies and retry mechanism.",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "prepublish:npm": "yarn build",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json -c tslint.json"
  },
  "keywords": [],
  "author": "Philipp Sumi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hardcodet/httpclient.git"
  },
  "bugs": {
    "url": "https://github.com/hardcodet/httpclient/issues"
  },
  "homepage": "https://github.com/hardcodet/httpclient#readme",
  "devDependencies": {
    "@types/node": "^13.13.2",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "tslint": "^6.1.1",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "date-fns": "^2.12.0"
  }
}
